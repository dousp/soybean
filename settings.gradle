rootProject.name = 'soybean'

include 'soy-demo'
include 'soy-code-gen'
include 'soy-auth'
include 'soy-route'

rootProject.children.each {project ->
    project.buildFileName = "${project.name}.gradle"
}